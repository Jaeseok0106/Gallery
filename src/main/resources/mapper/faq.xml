<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.human.gallery.domain.faq.ifaq">
    <select id="listfaq" resultType="com.human.gallery.domain.faq.faqDTO">
        select a.id, a.title, a.content, b.id as userid, a.categoryList, a.heart, a.postdate, a.views
        from board_post a, users b
        where categoryList=1
          and a.writer=b.usernum
        order by a.id desc
    </select>
</mapper>
